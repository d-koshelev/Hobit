# Contract Drift Decision Matrix

Contract status: Current Phase 1 decision matrix

Source of truth for:

- Phase 1 drift decisions and follow-up task direction

Not source of truth for:

- current widget behavior details
- implementation behavior
- future product roadmap

Related documents:

- `docs/ACTIVE_CONTRACT_INDEX.md`
- `docs/CURRENT_CODEBASE_STABILIZATION_PLAN.md`
- `docs/CURRENT_WIDGET_SURFACE.md`
- `docs/ARCHITECTURE.md`

This document records cleanup decisions for known contract drift and ambiguity
areas. It does not authorize code changes, component renames, persistence ID
renames, storage migrations, new runtime behavior, or new product surfaces.
`docs/CURRENT_WIDGET_SURFACE.md` remains authoritative for current widget
behavior.

## Decision Matrix

| Area | Current evidence | Drift / ambiguity | Phase 1 decision | Rationale | Next follow-up task | Status label | Must not do yet |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Agent Chat / Agent Monitoring / proposal-era APIs | Tauri commands and frontend API modules still reference `generate_agent_chat_ai_proposal`, `persist_agent_chat_proposal`, `get_agent_monitoring_snapshot`, and `create_agent_queue_item_from_proposal`; `CURRENT_WIDGET_SURFACE` excludes Agent Chat and Agent Monitoring as preferred catalog widgets and classifies proposal-era APIs as Compatibility or pending-retirement. | Older docs and retained API names can imply Agent Chat / Agent Monitoring are preferred current product widgets or part of the Coordinator / Executor model. | Keep as Compatibility / pending-retirement for now. Do not delete code in Phase 1 unless a later explicit code-removal task is approved. Do not treat Agent Chat / Agent Monitoring as preferred current product widgets. Future work should either retire/delete the old backend/frontend modules or keep them explicitly as compatibility APIs with narrowed docs. | Deleting these paths now risks broad code churn. The current goal is stabilization and contract clarity, not old-code removal. | Agent Chat / Agent Monitoring compatibility alignment: inventory the retained commands/modules and choose either narrow compatibility docs or an explicit removal task. | Compatibility / pending-retirement | Do not remove Tauri commands. Do not delete frontend APIs. Do not rename Coordinator / Queue / Executor surfaces. Do not rewrite orchestration behavior. |
| JDBC read-only execution / sidecar scope | `execute_jdbc_read_only_query`, `JdbcReadOnlyQueryPanel`, JDBC query DTO tests, current Architecture, and `CURRENT_WIDGET_SURFACE` show a shipped bounded mock/safe read-only validation/execution path; sidecar scaffold and runtime config exist but are not the default product runtime. | Some contract text still frames SQL execution as pending or future, while shipped code already has bounded mock execution. Real JDBC execution, credentials, sidecar activation, EXPLAIN, and Coordinator-triggered execution remain separate risks. | Accept shipped bounded mock/safe read-only SQL validation/execution as Current Preview. Real production JDBC execution remains Deferred. Coordinator-triggered hidden SQL execution remains Deferred. Credentials expansion, real sidecar production runtime, broad database automation, EXPLAIN workflows, and write SQL remain Deferred. | Shipped code already includes the preview UI/path. It is cheaper and clearer to document it as Current Preview than to remove it during Phase 1. The safety boundary must be explicit. | JDBC Preview contract alignment: update active JDBC-facing docs to call the bounded mock execution path Current Preview while keeping real database work Deferred. | Current Preview for bounded mock/safe path; Deferred for real JDBC execution | Do not expand JDBC capabilities. Do not let Coordinator execute SQL. Do not add credentials or production sidecar behavior. Do not remove JDBC execution path in this task. |
| Terminal PTY non-Windows behavior | `terminal_pty_unsupported` returns `Terminal PTY sessions are currently supported only on Windows desktop`; current docs state live PTY backend support is Windows-only and non-Windows creation returns unsupported-platform errors; tests are gated so non-Windows live PTY creation is not attempted as a lifecycle test. | The Terminal remains catalog-visible, while live PTY sessions fail on non-Windows desktop. The open decision is docs-only limitation versus platform catalog gating or cross-platform implementation. | Treat docs-only Windows-only limitation as sufficient for Phase 1. Keep catalog gating or cross-platform PTY support as later follow-up. Continue to ensure tests do not attempt unsupported non-Windows live PTY creation. | The cargo test blocker is already fixed. Implementing cross-platform PTY support or UI/catalog gating is beyond Phase 1. | Terminal platform limitation decision confirmation: confirm whether later work adds catalog gating, clearer unavailable UI, or Linux/macOS PTY support. | Current platform limitation; Deferred non-Windows live PTY support | Do not implement Linux/macOS PTY support. Do not change Terminal runtime behavior. Do not hide catalog entries in this task. |
| Smoke HTML entry points at Vite root | `coordinator-provider-product-smoke.html`, `jdbc-read-only-ui-smoke.html`, and `queue-executor-ui-smoke.html` are listed as Vite-root smoke/dev entry points; smoke React apps live under `src/smoke`. | Root-level smoke HTML can look like production entry points or product surfaces. The cleanup target and Vite handling are not yet settled. | Move smoke HTML files out of the Vite root in a later narrow cleanup task. Preferred target: a `smoke/dev` or `dev-smoke` subtree, with updated smoke instructions. Do not treat smoke HTML files as current product surfaces. | Vite root should remain production-focused. Smoke entry points are useful but should be clearly dev/test-only. | Smoke HTML root cleanup: move files and update smoke instructions without removing smoke coverage. | Deferred narrow cleanup | Do not move files in this decision-matrix task. Do not change Vite config in this task. Do not remove smoke coverage. |
| Pre-existing unrelated dirty worktree / broad diff-check failures | `git status --short --branch` shows many pre-existing unrelated dirty docs, frontend, Rust, and decision files; the stabilization plan already warns not to stage or modify unrelated dirty files. | Broad validation or diff cleanup can mix unrelated edits with focused Phase 1 work. | Treat as local pre-existing unrelated worktree state unless a later task explicitly scopes cleanup. Continue using scoped diff checks for Codex tasks. Do not touch unrelated dirty files during scoped tasks. | Current stabilization commits have avoided unrelated changes. Broad cleanup risks mixing unrelated work. | Smoke checklist discipline and scoped validation discipline: document per-task changed-file validation and dirty-worktree handling. | Current local constraint / scoped-task discipline | Do not fix unrelated whitespace in the same task as product/docs work. Do not stage unrelated dirty files. |
| Widget bloat / Placeholder naming mismatch | `WidgetHost` maps placeholder-named components such as `AgentRunPlaceholderWidget`, `AgentQueuePlaceholderWidget`, `InteractiveAgentPlaceholderWidget`, `TerminalPlaceholderWidget`, and `NotesPlaceholderWidget`; `CURRENT_WIDGET_SURFACE` classifies these names as Compatibility. Prior audits flag large widgets and `WorkbenchCanvas` responsibility breadth. | Placeholder names can imply real shipped UI is still placeholder-only. Splitting or renaming now could collide with persistence compatibility and current stabilization. | Keep as P2 structural cleanup after Phase 1 baseline and Notes stabilization. Do not rename components or split widgets in the decision-matrix task. Notes should be the first reference refactor after Phase 1. | Large widgets are real change-amplification sources. Renaming/splitting now would mix stabilization with refactor. | Notes UI/controller refactor first, then targeted widget size/name cleanup without persistence ID renames. | P2 structural cleanup; Compatibility names retained | Do not rename PlaceholderWidget components. Do not split Coordinator / Queue / Executor yet. Do not start broad WorkbenchCanvas refactor. |
| WorkspaceApi / WidgetRenderProps / global action bag coupling | `workspaceApiTypes`, `tauriWorkspaceApi`, `memoryWorkspaceApi`, `WidgetRenderProps`, `widgetHostRenderProps`, and `useWorkbenchWidgetActions` carry many cross-domain actions; prior audits flag broad `WorkspaceApi`, `WidgetRenderProps`, and action-bag coupling. | Broad props and APIs make every widget appear to know about unrelated capabilities, increasing change amplification and accidental cross-widget coupling. | Keep as P2 structural cleanup after current contracts and Notes are stable. Start with NotesApi / Notes controller before broad API split. | Broad API split is risky before current surface and Notes contracts are stable. A smaller Notes slice gives a safer reference pattern. | WorkspaceApi / WidgetRenderProps / action bag cleanup after Notes refactor, starting with the narrowest widget API extraction. | P2 structural cleanup | Do not split the entire WorkspaceApi in one task. Do not refactor all widget props at once. |
| Manual Rust/Tauri/TypeScript DTO duplication | DTO mapping and tests exist across Rust app/Tauri/frontend boundaries; current docs and audits call out manual Rust/Tauri/TypeScript shape drift risk. | Type drift can break bridge behavior, but a code generation stack would add dependency and process churn. | Record as P2/P3 follow-up. Prefer DTO shape tests or a narrow Notes DTO test before attempting code generation. | DTO generation is useful, but not required to finish Phase 1 stabilization. Narrow tests reduce drift risk with less process cost. | Feature-slice checklist: require DTO shape checks for bridge-touching tasks; consider narrow DTO tests before generation. | P2/P3 follow-up | Do not introduce a new type generation stack in Phase 1 without explicit approval. |
| hobit-agent / repo process hygiene | Root `Cargo.toml` includes `crates/hobit-agent`; Architecture describes `hobit-agent` and `hobit-tools` as placeholder crates; current validation baseline says `cargo check --workspace` and `cargo test --workspace` pass. | Placeholder crate presence can invite premature removal or workspace layout cleanup during unrelated tasks. | Keep as later repo hygiene follow-up. Do not remove crates or change workspace members during the decision-matrix task. | Cargo check and cargo test now pass. Removing crates or changing workspace layout could introduce noise. | Phase 1 closeout or later repo hygiene task: decide whether placeholder crates need documentation-only clarification or delayed removal. | Later repo hygiene follow-up | Do not edit Cargo.toml workspace members. Do not delete hobit-agent. |
| Coordinator / Queue / Executor naming and responsibility cleanup | Current docs define Coordinator Chat, Agent Queue, and Agent Executor as current/preview surfaces while retaining compatibility ids such as `interactive-agent` and `agent-run`; current surface says cleanup remains deferred. | Naming and responsibility overlap remains a real ambiguity source, especially with older Agent Chat / Agent Monitoring language. | Explicitly Deferred until after current codebase cleanup and Notes stabilization. When reached, start with naming contract docs only. Do not perform component renames or responsibility refactors during Phase 1 decision work. | The triad is important, but it should not distract from current baseline cleanup. | Coordinator / Queue / Executor naming and responsibility cleanup: start with a naming contract, then split implementation work into narrow follow-ups. | Deferred | Do not rename `interactive-agent` or `agent-run` IDs. Do not rename components. Do not create the naming contract in this task unless only listed as follow-up. |

## Recommended Follow-Up Sequence

1. Agent Chat / Agent Monitoring compatibility alignment
2. JDBC Preview contract alignment
3. Terminal platform limitation decision confirmation
4. Smoke HTML root cleanup
5. Smoke checklist discipline
6. Feature-slice checklist
7. Phase 1 closeout
8. Notes UI/controller refactor
9. Notes dev-only memory API
10. WorkspaceApi / WidgetRenderProps / action bag cleanup
11. Coordinator / Queue / Executor naming and responsibility cleanup
